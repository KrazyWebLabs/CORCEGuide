<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CORCEGuideApp.Views.MainCategoryView"
             xmlns:models="clr-namespace:CORCEGuideApp.Models"
             Title="MainCategoryView">
    <ScrollView>
        <VerticalStackLayout>
            <Label 
                Text="Avaible Categories"
                VerticalOptions="Center" 
                HorizontalOptions="Center" />
            <CollectionView
                ItemsSource="{Binding Categories}"
                SelectionMode="Single"
                SelectedItem="{Binding SelectedCategory}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border
                            Padding="10"
                            Margin="5">
                            <VerticalStackLayout>
                                <Label 
                                    Text="{Binding Name}"
                                    FontAttributes="Bold"/>

                            </VerticalStackLayout>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            <Button
                Text="Add New Category"
                VerticalOptions="End"
                Margin="0, 10, 0, 0"
                Clicked="OnAddNewCategory"/>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>